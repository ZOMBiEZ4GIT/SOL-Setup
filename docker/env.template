# Copy to docker/.env on the server and fill in real values. Never commit docker/.env.
TZ=Australia/Melbourne
PUID=1000
PGID=1000

# ===== Proton VPN (choose ONE stack) =====
# --- OpenVPN (default) ---
OPENVPN_USER=<your_proton_openvpn_user_or_appuser>
OPENVPN_PASSWORD=<your_proton_openvpn_password>
OPENVPN_PROTOCOL=udp
SERVER_COUNTRIES=Australia

# --- WireGuard (optional; comment OpenVPN above if you use this) ---
# WIREGUARD_PRIVATE_KEY=<wg_private_key>
# WIREGUARD_ADDRESSES=10.2.0.2/32
# SERVER_COUNTRIES=Australia
